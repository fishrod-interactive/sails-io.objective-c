#include "Pods-socket.IO.xcconfig"
GCC_PREPROCESSOR_DEFINITIONS = COCOAPODS=1
HEADER_SEARCH_PATHS = "${PODS_ROOT}/BuildHeaders" "${PODS_ROOT}/BuildHeaders/socket.IO" "${PODS_ROOT}/Headers" "${PODS_ROOT}/Headers/SocketRocket" "${PODS_ROOT}/Headers/socket.IO"
OTHER_LDFLAGS = -ObjC
PODS_ROOT = ${SRCROOT}